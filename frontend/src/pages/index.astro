---
import Layout from "../layouts/Layout.astro";
import Image from "astro/components/Image.astro";
---
<!-- TODO: Handle routes to some views -->

<Layout title="qué mira sap@ hp">
  <main>
    <div class="title">HOLA SORRITAS 😘</div>
    <div class="description">
      <p>
        Bueno, aqui vas a poder usar el bot <strong>SORRA</strong>, tendrás una
        lista de opciones de comandos, solo es dar click, no es muy duro
      </p>
    </div>
    <div class="content">
      <button id="track" class="btn"> Track 😈 </button>
      <button id="report" class="btn"> Report 🥵 </button>
      <button id="wishlist" class="btn"> Wishlist 🤑 </button>
      <div class="footer">
        <Image src="/bola-amarilla.svg" alt="bolita" width="100" height="100" />
      </div>
    </div>
  </main>

  <script>
    const trackBtn = document.getElementById("track");
    const reportBtn = document.getElementById("report");
    const wishlistBtn = document.getElementById("wishlist");

    if (trackBtn !== null) {
      trackBtn.addEventListener("click", () =>
        console.log("track button clicked")
      )
    }

    if (reportBtn !== null) {
      reportBtn.addEventListener("click", () =>
        console.log("report button clicked")
      )
    }

    if (wishlistBtn !== null) {
      wishlistBtn.addEventListener("click", () =>
        console.log("wishlist button clicked")
      )
    }
  </script>

  <style>
    main {
      margin: auto;
      padding: 1rem;
      width: 100%;
      max-width: calc(100% - 2rem);
      color: black;
      font-size: 20px;
      line-height: 1.6;
      background-color: white;
      height: 100%;
      display: flow;
    }
    .text-gradient {
      background-image: var(--accent-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-size: 400%;
      background-position: 0%;
    }
    .content {
      margin: 0.5rem;
      width: 100%;
      justify-content: center;
      display: grid;
    }
    .description {
      padding: 0.5rem;
      margin-left: 5%;
      font-size: 15px;
      font-weight: 300;
      line-height: 22.5px;
      text-align: center;
      text-underline-position: from-font;
      text-decoration-skip-ink: none;
    }
    .title {
      font-size: 3rem;
      font-weight: 700;
      line-height: 1;
      text-align: center;
      margin-bottom: 0.5rem;
      font-family: "Poppins";
    }
    .btn {
      width: 300px;
      height: 45px;
      border-radius: 10px;
      margin: 0.8rem;
      background-color: #883aeacc;
      font-family: "Poppins";
      font-size: 15px;
      font-weight: 500;
      line-height: 22.5px;
      text-align: center;
      text-underline-position: from-font;
      text-decoration-skip-ink: none;
      color: white;
      size: 15px;
      box-shadow:
        0 8px 16px 0 rgba(0, 0, 0, 0.2),
        0 6px 20px 0 rgba(0, 0, 0, 0.19);
      cursor: pointer;
      border-color: transparent;
    }

    .instructions {
      margin-bottom: 2rem;
      border: 1px solid rgba(var(--accent-light), 25%);
      background: linear-gradient(
        rgba(var(--accent-dark), 66%),
        rgba(var(--accent-dark), 33%)
      );
      padding: 1.5rem;
      border-radius: 8px;
    }
    .instructions code {
      font-size: 0.8em;
      font-weight: bold;
      background: rgba(var(--accent-light), 12%);
      color: rgb(var(--accent-light));
      border-radius: 4px;
      padding: 0.3em 0.4em;
    }
    .footer {
      position: absolute;
      justify-content: center;
      width: 100%;
      display: flex;
      bottom: 0;
    }
  </style>
</Layout>
